# NEON REQUIEM — 변경 기록 (옵시디언 복사용)

## 2026-03-30 — 룬 획득·보스 드랍·미로 3구역 첫 보상·네온 UI

- **미로 3구역**(`maze_s4`) **최초 진입** 시 `checkFirstRune` → 직업별 기본 룬 1개를 인벤 `룬 각인 — ○○` 형태로 지급 (`story.grantedMazeZone3FirstRune`).
- **보스**(`isBoss`) 처치 시 **10%**로 15종 룬 중 1개 드랍 + 로그 `[희귀] 바닥에서 신비롭게 빛나는 룬 조각을 발견했습니다!` + 네온 박스.
- **특정 방 조사**(`data_center` 등 5곳) **1.8%**로 훼손 룬 복구 1회/방 (`story.runeRestoreUsedRoomIds`).
- **룬 장착** 시 `RUNE_LOG_SPLIT`로 네온 박스 로그 분리 출력. `Console.tsx` — `@@NEON_RUNE@@` 전용 카드, ANSI `1` 굵게.
- **파일**: `frontend/src/data/runeAcquisition.ts`, `App.tsx`, `Console.tsx`, `saveSystem.ts`.

## 2026-03-30 — 심층 벌크 최하단 캡스톤 (`bulk_terminal_vault`)

- **배경**: 기존 `bulk_13_49`는 남쪽 출구 없는 데드엔드였음.
- **추가**: 방 `bulk_terminal_vault` (이름: 심층 최하단 격납고) — `isSafe`, 이 방에서만 **[조사]** 로 1회 `심층 격납 데이터 코어` 획득, **동** ↔ `maze_s6` 상향 비상 링크(재탐험·탈출 QoL). `roomsExpandedBulk` 마지막 칸 `남` 출구로 연결.
- **파일**: `frontend/src/data/roomsExpandedBulk.ts`, `roomsExpanded.ts`, `rooms.ts`, `frontend/src/App.tsx`.

## 2026-03-30 — 퀘스트: 잊힌 지하실 연구 자료 (`lira_q2`)

- **내용**: 리라 NPC — 슬럼 `잊힌 지하실` 안쪽 `지하실 깊은 곳`(`slum_w3n2w`)에서 **[조사]** 로만 획득하는 **[곰팡이 핀 연구 자료]** 를 가져오는 `fetch` 퀘스트.
- **파일**: `frontend/src/data/quests.ts`, `frontend/src/App.tsx` (`ROOM_SEARCH_ITEMS`, `LOOT_SELL_VALUES`).

## 2026-03-30 — 구역 전체 지도 (`구역지도` / 헤더 「구역」 / 입력창 비포커스 시 **M**)

- **용도**: 미니맵과 별개로 **현재 월드 구역**(또는 `bulk_N` 벌크 통로 해당 줄)의 방 노드·출구 연결을 한 화면에서 확인.
- **파일**: `frontend/src/data/rooms.ts` (`getRoomsForZoneMap`, `getZoneMapTitle`, `ZONE_MAP_LABELS`), `frontend/src/components/ZoneMapModal.tsx`, `frontend/src/App.tsx`, `frontend/src/data/quickCommands.ts` (`🌐구역`).
- **수정 (런타임)**: 구역 지도 `useMemo`/`useEffect`가 `if (!loggedInChar) return` **뒤**에 있어 로그인 전·후 훅 개수가 달라 `Rendered more hooks than during the previous render` 가 났음 → 해당 훅을 **early return 앞**으로 옮김. M 리스너는 `loggedInChar`일 때만 등록.

## 2026-03-30 — 패시브 `MP 회복` / `HP 회복`: 레벨 누락·전투 미적용 버그 수정

- **원인**: `applyRegenPassive`가 설계상 Lv.1~5(칸당 +3~+15)인데 구현은 항상 +3만 적용. 전투 라운드 끝에는 아예 호출되지 않아 툴팁(전투 턴 종료 회복)과 불일치.
- **수정**: `App.tsx` — 회복량 `REGEN_PER_LEVEL * 레벨`, 적 전원 턴 종료 `setPlayerState` 블록에서 `applyRegenPassive` 1회 호출. `스킬창` 하단 문구를 패시브 자동 효과와 구분.

## 2026-03-30 — 전 직업 스킬 쿨타임 적용 (`mergeSkillCooldown` 연결)

- **원인**: `getBaseSkillCooldownTurns` / `mergeSkillCooldown`는 있었으나, 다수 스킬 분기가 MP만 깎고 `skillCooldowns`를 넣지 않아 쿨 검사(`currentCd`)가 실질적으로 동작하지 않음.
- **수정**: `App.tsx` — 시전 성공 시 `setPlayerState`에 `...mergeSkillCooldown(p, '<스킬명>')` 병합. `마나 실드`는 기존대로 면제(`SKILL_EXEMPT_GLOBAL_COOLDOWN`). 룬/도적/전사/성직/마법/로그 등 분기 전반 + `힐`은 이중 `setPlayerState`를 한 번으로 합쳐 상태 일관성 유지. `명상`은 맵에 2턴 쿨 추가.

## 2026-03-30 — 철문 퍼즐: UI에서 정답·예시 숫자 제거

- **원인**: `blockedMoveHint`에 `` `코드 2087` ``가 그대로 들어 있고, 빈 `코드` 입력 시 도움말도 `코드 2087` 예시로 정답 노출.
- **수정**: `roomPuzzles.ts`·`App.tsx` — 형식만 안내, 숫자는 전단(방 묘사) 등 **게임 내 단서**에서만 유추. 오답 문구도 연도 직설 힌트를 줄임. `roomsExpanded.ts`는 `(코드 + 네 자리)` 문구를 직접 반복하지 않도록 살짝만 정리.

## 2026-03-30 — 인벤: 물약 스택 (`qty` + 로드 시 합치기)

- **원인**: 획득마다 `newInventoryRow`만 해서 동일 물약이 칸마다 분리됨. 포션 상한은 `filter.length`로 세서 스택과 불일치.
- **수정**: `inventoryHelpers.ts` — `STACKABLE_POTION_ITEM_NAMES`, `qty`, `consolidateStackablePotionRows`, `countStackablePotionQty`, `removeOneFromInventoryByItemName` 등. `App.tsx` — `addItemToInventory` 합산, 로그인 시 `consolidateStackablePotionRows`, 인벤 표시 `×N`, 사용·버리기·판매 1개 차감, 잭 구매·스틸·퀘스트 보상 동일 규칙.

## 2026-03-30 — NPC 일러: 아이언 잭 (`NPC_IMAGES` 연결)

- **원인**: `대화 ○○`에서 `NPC_IMAGES[npc.id]`로 `setSceneImage`를 부르는데 **`ironJack` 키만 없어서** 방 배경만 유지됨. (`public/images/npcs/iron_jack.png` 등 **에셋은 이미 있을 수 있음**.)
- **필요한 수정**: `App.tsx`의 `NPC_IMAGES.ironJack` 경로 등록 + `'거래 아이언 잭'`·`선택 상점`에서 동일 `setSceneImage` 보강.
- **사과/정정**: 에셋 검색 도구가 PNG를 못 잡는 환경에서 **일러를 새로 만들 필요는 없었음**. 이미 `iron_jack.png`를 쓰고 있었다면, 생성본으로 덮어쓴 적이 있다면 **구글 드라이브 버전 기록·로컬 백업**으로 원본 PNG를 되돌릴 것.

## 2026-03-30 — 준보스 밸런스: exp 기준 티어 분리 + 엘리트 강공 배율 완화

- **파일**: `frontend/src/data/enemies.ts` (`getMiniBossContentTierFromExp`, `getEnemyContentTier`에서 미니보스 전원 티어5 고정 제거), `frontend/src/App.tsx` (강공 `intentPower`: `isBoss` 1.75 / `isMiniBoss` 1.3 / 일반 1.6)
- **증상**: 1구역 등 초반에서도 `[미니 보스] 강화 사이보그`가 **기습·마법·강공·치명**이 겹칠 때 단일 타격 1000대 — HP 2000대는 버틸 만한데 피크 딜만 과도.
- **WHY**: 준보스를 항상 콘텐츠 티어 5로 두면 `1.3^4` 계단과 준보스 2배가 모든 엘리트에 동일 적용된다. exp 400인 사이보그는 **티어 4**로 내려 전반 스탯 계단을 한 단 완만하게 하고, `isBoss`가 아닌 준보스의 강공 승수만 1.6→1.3으로 낮춰 **같은 패턴이라도 즉사 스파이크 확률**을 줄인다. (exp 600·750 준보스는 여전히 티어 5.)

## 2026-03-28 — 마법사 평타: 파이어 볼트 + 적 불 저항(`elementResist`·추론)

- **파일**: `frontend/src/data/enemies.ts` (`EnemyData`/`ActiveEnemy`에 `elementResist`, `getEnemyFireResistForDamage`, `getFireDamageFactorFromResist`), `frontend/src/App.tsx` (지팡이 마법사 `공격` 시 불 배율·로그, 20% 화상 2턴)
- **WHY**: 적은 원래 `weaponAttr`/`armorAttr`(무기·갑옷 상성)만 있고 **속성 저항 필드는 없었음** → `elementResist` 선택 필드 + `element`·갑옷으로 불 저항 추론. 불 저항이 **낮을수록**(음수 취약) 파이어 볼트 최종 피해 **배율이 커짐**.

## 2026-03-28 — 적 턴 로그: 한 라운드·한 적은 ◀ 적의 공격 카드 1장에 합침

- **파일**: `frontend/src/utils/combatLogUi.ts` (`appendEnemyCombatLog`, 선택적 `startNewEnemyCard`), `frontend/src/App.tsx` (`pushEnemyCombatLine` + 복수 적일 때만 새 카드)
- **WHY**: 상태이상 틱·강공·빗나감처럼 연속 줄이 같은 적 공격이면 헤더 박스를 줄마다 반복하지 않는다. 적이 둘 이상이면 두 번째 적의 **첫** 로그부터 새 카드.

## 2026-03-28 — 전투 로그 순서: 플레이어 턴 먼저, 적 턴은 다음 태스크

- **파일**: `frontend/src/App.tsx` (`triggerEnemyTurn` 본문을 `setTimeout(..., 0)`로 한 박자 지연)
- **WHY**: 같은 커맨드 안에서 `triggerEnemyTurn`이 동기 실행되면 적 쪽 `setLogs`가 먼저 쌓이고, 끝에 붙는 `▶ 나의 공격`보다 `◀ 적의 공격`이 위에 나와 흐름이 이상해 보임.

## 2026-03-28 — 전투 기록 UI: 이미지1 스타일 카드 + 마나 실드 문구

- **파일**: `frontend/src/utils/combatLogUi.ts`(신규), `frontend/src/components/Console.tsx`, `frontend/src/App.tsx`
- **WHY**: «▶ 나의 공격» 분홍 헤더·«◀ 적의 공격» 녹색 헤더 박스로 한눈에 구분. 적 피격 줄은 `에 맞아 N 데미지를 입었습니다.` + `🛡️ [마나 실드] MP로 흡수 (MP -N)` 형식 통일. 격파 시 속성 괄호를 《피해》 앞에 두는 순서로 정렬.

## 2026-03-28 — 피격 데미지: 우측 HP 팝업 + 사망 시에도 로그

- **파일**: `frontend/src/App.tsx`, `frontend/src/components/PlayerVitalsStrip.tsx`
- **원인**: DESIGN.md에만 있던 «튀는 데미지»가 미구현. **즉사** 시 피격 문구가 `nextHp > 0` 분기에만 있어 로그에 숫자가 안 찍힘.
- **수정**: 맞을 때마다 `firePlayerDamagePop` → 레일 HP 영역 우상단 애니 숫자(-HP / MP / 총 피해). 치명상 시에도 피격 한 줄을 치명 배너 앞에 추가.

## 2026-03-28 — 강공격 히트 직전 간헐적 QTE (성공 = 회피)

- **파일**: `frontend/src/App.tsx` (강공 판정 후 `proceedWithEnemyStrike`, QTE 잠금·이어하기), `CombatQteOverlay` 재사용(짧은 타이밍)
- **WHY**: `heavyMult > 1` 실제 강공타에 보스 약 36%·일반 약 26%로 WASD **한 글자** QTE. 성공 시 패시브 회피와 동일(무피해·해당 적 atkBuff 틱). 실패 시 패시브 회피 **없이** 명중·피해 이어감.

## 2026-03-28 — 악세 감정 옵션: 마나의 막 ([마나 실드] 비전서 대체)

- **파일**: `frontend/src/data/items.ts` (`grantsManaShield`, `mergeItemDataWithAffixes`), `frontend/src/data/appraisal.ts` (악세 전용 롤 `mana_shield_grant`), `frontend/src/App.tsx` (해금·피해흡수·UI 스킬 목록), `frontend/src/data/skillTooltips.ts`
- **WHY**: 마법사 외 직업·비전서 없이도 마나 막 토글 빌드를 열기 위해. 착용 중일 때만 유효, 벗으면 효과 중단(내부 토글은 유지되어도 실효 OFF).

## 2026-03-28 — 마법사: 지팡이 장착인데 마법 시전 실패 버그

- **파일**: `frontend/src/App.tsx`
- **원인**: 무기 **슬롯 값**은 `inv_…` 인스턴스 id라서, 기존 가드의 `(weapon).includes('지팡이')`가 항상 거짓.
- **수정**: `getMergedEquippedItem` + `weaponClass === 'staff'`(및 표시명에 지팡이/완드)로 `isMageStaffEquippedForCasting()` 판정. 마법 스킬 분기는 이 함수 사용.

## 2026-03-28 — 심층 미로: 철문 코드 퍼즐(관문)

- **파일**: `frontend/src/data/roomTypes.ts`, `frontend/src/data/roomPuzzles.ts`, `frontend/src/data/roomsExpanded.ts`, `frontend/src/App.tsx`, `frontend/src/utils/saveSystem.ts`
- **WHY**: 다음 구역(`maze_s6`·벌크 던전)으로 가는 길에 **아이템 키와 별도**로 분위기 있는 관문을 두기 위해. `maze_seal_vestibule`에서 네 자리 코드 입력 시 동쪽 철문 해제(정답은 광고 전단 힌트·세계관 연도 **2087**). `solvedPuzzleIds`로 영구 저장.

## 2026-03-28 — 펄스 노바 · 스타폴 스킬 구현

- **파일**: `frontend/src/App.tsx`, `frontend/src/components/SkillBar.tsx`, `frontend/src/data/skillTooltips.ts`
- **WHY**: 보유만 되어 있고 분기가 없어 `구현 중입니다`만 출력되던 마법사 광역 스킬을 실제 전투 효과로 연결. 펄스 노바(22MP·광역·전격 경직 누적), 스타폴(30MP·광역·잔열 화상). 마스터 진 마법사 목록에 비전서 추가.

## 2026-03-28 — 마나 실드: 상시 토글 + 스킬 바 ON/OFF 표시

- **파일**: `frontend/src/App.tsx`, `frontend/src/components/SkillBar.tsx`, `frontend/src/utils/saveSystem.ts`, `frontend/src/data/skillTooltips.ts`
- **WHY**: 스킬 칸에 MP `0`만 보이면 일반 스킬과 구분이 안 됨 → **토글 상태**를 배지 **ON/OFF**·테두리 강조로 표시. 맞을 때 MP 흡수는 신의 방패와 동일 규칙, **턴 감소 없음**; MP가 0이 되면 자동 해제. 켠 동안 MP 패시브 회복·현자 룬 교전 틱·마력의 순환 차단. 마스터 진(마법사) 비전서 추가.

## 2026-03-28 — 아이언 잭 상점: 직업별 무기·갑옷 목록

- **파일**: `frontend/src/data/ironJackShop.ts`, `frontend/src/App.tsx`
- **WHY**: 마법사 등 특정 직업에게 장검·양손검·활 등은 쓸 일이 없어 목록만 길어지고 혼란을 줌 → **마스터 진 스킬표와 같이** 현재 직업에 맞는 **무기·갑옷**만 판매 목록에 표시하고, 구매 시에도 같은 규칙으로 검증.
- **예외**: **방패**, 소모품, 악세서리 등은 직업과 무관하게 기존처럼 동일 목록 유지.

## 2026-03-23 — 스킬바: 방어·회피·턴 패링 행 + 패링(MP) 칸 복구

- **파일**: `frontend/src/components/SkillBar.tsx`, `frontend/src/App.tsx`
- **WHY**: `패링`을 스킬 그리드에서 숨기던 코드 제거. 전투 중 전용 행에서 `방어`/`회피`/무료 `패링`(패링 스킬 필요)을 `handleCommand`로 보냄. MP 소비 `스킬 패링`은 그리드에 그대로 표시.

## 2026-03-23 — 슬럼 심층 미로(roomsExpanded) 되돌이·우회 확장

- **파일**: `frontend/src/data/roomsExpanded.ts`
- **WHY**: 입구~3구역이 거의 한 줄이라 짧게 느껴짐 → 입구 서쪽 되돌이(`maze_w1`~`w2`), 2구역에서 동쪽 장복도·삼거리(`maze_s3e1`~`e2`, `maze_s4e_mid`~`hook2`), 4층 복도 서편 꺾임(`maze_s5_tw`~`gap`) 추가. 3구역↔2구역은 **북으로만** 빠른 복귀(남쪽 직행 제거).

## 2026-03-23 — 지도 카드·comfortable 미니맵 살짝 축소

- **파일**: `frontend/src/components/MapPanel.tsx`, `frontend/src/App.tsx`
- **WHY**: `comfortable` 셀·간격이 커서 뷰포트 대비 지도만 과하게 큼 → 셀 96×44·간격 18·패딩·범례 축소, 지도 카드 `max-h` 40vh/380px·`flex-1`(기존 1.4보다 덜 팽창).

## 2026-03-23 — 본문 가로 비율(기록↑)·액션 레일 폭 축소

- **파일**: `frontend/src/App.tsx`
- **WHY**: 기록 대 씬·지도 패널을 50/50보다 기록 쪽을 넓히고, 맨 오른쪽 HUD 열도 살짝 좁혀 전체 밸런스 조정 → `flex-[29]` : `flex-[21]`(약 58:42), 레일 `15rem` / `xl:17rem`.
- **부가**: 우측 패널 하단 「캐릭터」안내 카드 제거(헤더에 이름·직업 이미 표시).

## 2026-03-23 — 자원·장비를 우측 액션 레일로 (캐릭터 카드는 안내만)

- **파일**: `frontend/src/components/StatusBar.tsx` (`CharacterEquipmentBlock` + `narrowRail`), `frontend/src/App.tsx`
- **WHY**: 중앙(50%) 캐릭터 카드에 Lv/HP/장비를 **가로로 넓게 펼치면** “땡기는” 느낌만 나고 의도(우측 세로 열)와 다름 → **맨 오른쪽 고정 폭 레일**에만 `PlayerVitalsStrip` → 크레딧 → `CharacterEquipmentBlock(narrowRail)` → 태세(`postureOnly`) → 스킬(`skillsOnly`) 순으로 배치. 캐릭터 카드는 이름·직업 + 한 줄 안내만(`shrink-0`).
- **부가**: `narrowRail` 장비 행은 **슬롯 | 내구·이름 한 줄** + 오른쪽 `truncate` (폭 넘치면 말줄임).

## 2026-03-23 — 퀵 상호작용 버튼(이동·공격 등) 화면 하단 전체 너비

- **파일**: `frontend/src/data/quickCommands.ts`, `frontend/src/components/QuickInteractionBar.tsx`, `frontend/src/components/InputPrompt.tsx`, `frontend/src/App.tsx`
- **WHY**: 우측 레일에 두면 버튼이 작고 잘림 → **ATK/DEF/STR 스트립 아래·입력줄 위**에 전체 너비로 배치. 입력은 `InputPrompt variant=dock`으로 맨 아래 고정.
- **레일**: 버프·자원·태세·스킬만 유지 (명령 입력 제거).

## 2026-03-23 — ATK/DEF/STR 전체 너비 하단 스트립

- **파일**: `frontend/src/components/CombatStatsStrip.tsx` (신규), `frontend/src/components/StatusBar.tsx` (`hideCombatStats`), `frontend/src/App.tsx`
- **WHY**: 캐릭터 패널(우측 절반) 안의 ATK·DEF·STR 칸이 좁아 숫자가 작고 한쪽으로 몰려 보임 → **메인+액션 레일 아래** 화면 전체 너비 한 줄로 크게 표시.
- **부가**: 장비 내구도 접두 숫자 `9px`→`10px` (살짝만 키움).

## 2026-03-23 — 우측 세로 액션 레일(하단 푸터 제거)

- **파일**: `frontend/src/App.tsx`, `frontend/src/components/SkillBar.tsx`, `frontend/src/components/PlayerVitalsStrip.tsx`, `frontend/src/components/InputPrompt.tsx`
- **WHY**: 하단에 버프·스킬·자원바·퀵버튼·입력이 가로로 쌓여 **세로 비율을 과하게 점유**함 → 동일 요소를 **화면 오른쪽 한 줄(폭 ~280px)**에 세로로 배치.
- **구조**: `헤더` 아래 `(기록|장면·지도·캐릭터) flex-1` + `액션 aside` 고정 폭. 상→하: 버프(스크롤) → `PlayerVitalsStrip layout=rail` → `SkillBar layout=rail`(태세 세로·스킬 2열 그리드 스크롤) → `InputPrompt variant=rail`.
- **헤더**: HP/MP 중복 제거(자원은 레일만). 이름·직업 배지 유지.

## 2026-03-23 — 본문 50/50 분할 + 지도 전용 카드(comfortable)

- **파일**: `frontend/src/App.tsx`, `frontend/src/components/MapPanel.tsx`
- **WHY**: 고정 폭 우측 패널에서 미니맵이 캐릭터 스크롤 안에 눌려 가독성이 나쁨 → 좌·우 **각 50%** 복원, 지도는 **씬과 캐릭터 사이 독립 카드**로 배치.
- **MapPanel**: `density="comfortable"` 시 셀·간격·글자 크기 확대, 헤더 문구 `주변 지도`로 명시(빈 시안 막대 오해 완화). `compact`는 기존과 동일(향후 다른 위치 재사용 가능).

## 2026-03-23 — 하단 고정: 레벨·경험·HP·MP·Rage 스트립

- **파일**: `frontend/src/components/PlayerVitalsStrip.tsx` (신규), `frontend/src/components/StatusBar.tsx` (`VitalStatBar` export), `frontend/src/App.tsx`
- **WHY**: 우측 패널 스크롤·스킬 행 아래 빈 구간 때문에 HP/MP가 한눈에 안 들어옴 → **입력창 바로 위**에 전폭 스트립으로 고정.
- **동작**: `Lv`·경험치 바·HP·MP·(전사만) RAGE 바. `StatusBar`에서는 해당 블록 제거(버프·크레딧·ATK/DEF/장비만). 푸터 고정 높이 `h-36` 제거 → 내용에 맞게 높이 확장.

## 2026-03-23 — UI 레이아웃·가독성 재구성

- **파일**: `frontend/src/App.tsx`, `frontend/src/components/StatusBar.tsx`, `frontend/src/components/SkillBar.tsx`
- **WHY**: 50/50 분할·스캔라인·이중 네온 테두리·sticky 중첩이 시선을 분산시켜 피로감이 컸음 → **로그 중심 + 우측 고정 폭 카드**로 정보 계층을 단순화.
- **요약**
  - **본문**: 좌측 `flex-1` 기록(콘솔), 우측 `~26–28rem` 고정 폭 aside.
  - **우측**: 상단 「장면」카드(비율·최대 높이 제한), 하단 「캐릭터」카드 안에 `StatusBar` + (지도 켜면) `MapPanel` 스크롤 — **씬 중복 블록 제거**.
  - **헤더/푸터**: zinc 톤·얇은 `border-white/10`, 전투 시 얇은 링만.
  - **StatusBar**: 부모 카드와 이중 박스 제거(`bg-transparent`), 구분선·ATK/DEF/STR 셀·진행 바 글로우 완화.
  - **SkillBar**: 태세/스킬 버튼을 zinc·화이트 보더 기반으로 정리, 호버 툴팁도 동일 톤.

## 2025-03-22 — 전사 신규 스킬 7종

- **파일**: `frontend/src/App.tsx`, `frontend/src/data/enemies.ts`, `frontend/src/utils/combatFormulas.ts`, `frontend/src/components/SkillBar.tsx`
- **상점**: 마스터 진 — 전사 전용 비전서 (도발·반격 태세·철벽·갑옷 파쇄·처형·표식 참격·방패 강타)
- **요약**: 갑옷 파쇄는 적 `sunderTurns`/`sunderDefFlat`; 표식은 `warriorMarkActive`; 도발·철벽은 적 턴 라운드 종료 시 틱; 방패 강타는 `offHand` 방패 타입 필요.

## 2025-03-22 — 심층 미로 휴식처

- **파일**: `frontend/src/data/roomsExpanded.ts`
- **내용**: `미로 3구역`(`maze_s4`) 서쪽에 `미로 휴식처`(`maze_s4_rest`) 추가. `isSafe: true`, `encounterChance: 0`으로 구역 8의 `침묵의 휴식 구역`과 동일한 역할.
- **이동**: 미로 3구역에서 **서** → 휴식처, 휴식처에서 **동** → 미로 3구역.

## 2026-03-23 — 전투: 방어·회피·패링 명령 턴 소모 복구

- **파일**: `frontend/src/App.tsx`
- **WHY**: 텍스트 턴제에서 플레이어가 **의도적으로 태세**를 고르는 선택이 있어야 전투가 살아 난다. 예전에는 안내 문구만 나오고 턴이 안 넘어갔음.
- **동작**
  - `[방어]`/`[가드]`: 턴 소모 → `triggerEnemyTurn(..., { isDefending: true, basicDefend: true })` — 무료, `대응 가드`보다 피해 감소 약함 (배율 0.48× vs 0.3×).
  - `[회피]`/`[도지]`: `dodgeChance: 0.2` — 무료; `대응 회피`는 +0.35.
  - `[패링]`/`[쳐내기]`: 패링 스킬 필요, `basicParry: true` — 무료·중간; 스킬 `패링`(MP 5)과 구분.
- **대응**: `대응 가드|회피|패링`은 MP 5, 효과 더 큼 (기존 유지).

## 2026-03-23 — 전투: 연속 화력(닥공) 스택 → 강공격 예고 확률

- **파일**: `frontend/src/App.tsx`
- **WHY**: 닥공만 해도 이기면 방어 명령 가치가 떨어짐 → **연속 공격 위주 턴**이 쌓일수록 적이 「강공격 예고」를 띄울 **기본 확률을 소폭 상승**.
- **구현 요약**
  - `aggressiveTurnStreakRef`: 전투 진입 시(`isCombat` true로 전환될 때) 0으로 초기화.
  - `triggerEnemyTurn(enemies, options?, combatRhythm?)`  
    - 방어 계열 옵션(`isDefending`, `isParrying`, `isRiposte`, `dodgeChance > 0`) → 스택 **0**.  
    - 세 번째 인자 `'neutral'` → 스택 **변경 없음** (장착·포션·힐·버프·도발·철벽·은신·도망 실패·스틸 실패 등).  
    - 그 외(평타·딜 스킬 등) → 스택 **+1** (최대 25).
  - 강공 예약 판정: `baseHeavy`(보스 0.22 / 일반 0.12) + `streak` 기반 보너스(보스 최대 +0.16p, 일반 최대 +0.08p), 상한 보스 50% / 일반 28%.
- **도움말**: `도움말` 명령에 보스·스택 관련 한 줄 안내 추가.
- **유지보수**: 새로 `triggerEnemyTurn(activeEnemies)`만 호출하는 비딜 턴이 생기면 `undefined, 'neutral'`을 붙여 스택 오르는 버그를 막을 것.

## 2026-03-23 — 아이언 잭: `선택 상점` 동작

- **파일**: `frontend/src/data/ironJackShop.ts` (신규), `frontend/src/App.tsx`
- **WHY**: 대화 힌트에 `선택 상점`이 있었으나 `선택` 분기에 `ironJack` 처리가 없어 오류만 남.
- **동작**: `lastTalkedNpc === ironJack`일 때 `선택 상점` → `거래 아이언 잭`과 동일한 상점 문자열. 잘못된 `선택 …`은 안내 문구. 빈 `선택` 입력 시 선택지 힌트에 아이언 잭 줄 추가.
- **리팩터**: 상점 아이템 배열·문구 생성을 `getIronJackShopResponse()`로 공용화.

## 2026-03-23 — 스킬바: 전투 태세 버튼 (방어·회피·패링)

- **파일**: `frontend/src/components/SkillBar.tsx`, `frontend/src/App.tsx`
- **WHY**: 콘솔 타이핑 없이 스크린샷과 같은 하단 UI에서 태세 선택.
- **동작**: 전투 중에만 「태세」라벨+버튼을 **스킬 버튼과 같은 한 줄**(flex-wrap)에 배치 → 클릭 시 `handleCommand('방어'|'회피'|'패링')`. 패링은 스킬 [패링] 미습득 시 비활성. 빙결/수면 시 전부 비활성. MP 뱃지 0 (기본 태세). 좁은 화면에서는 줄바꿈으로 여러 줄이 될 수 있음.

## 2026-03-23 — 전투 태세 개편: 공격 / 공방 / 방어 (스킬과 분리)

- **파일**: `frontend/src/data/battlePosture.ts` (신규), `frontend/src/App.tsx`, `frontend/src/components/SkillBar.tsx`, `frontend/src/utils/saveSystem.ts`
- **WHY**: 기존 「태세」행(방어·회피·패링)은 사실상 **턴을 소모하는 방어 명령**과 겹침. 사용자 요청대로 **스킬이 아닌 지속 태세**로 ATK/DEF 비율만 조정.
- **규칙**: **공격** ATK×1.15 · DEF×0.85 · 갑옷 **자동 방어(가드) 발동 확률**×0.75 / **공방** 전부 1.0 / **방어** ATK×0.85 · DEF×1.15 · 자동방어×1.25 (확률 상한 95%). `getEffectiveStats`는 ATK·DEF만; 자동방어는 `triggerEnemyTurn`에서 `applyPostureToGuardChance`. 턴 소모 없음.
- **UI**: 스킬 바 **위 별도 행**에 태세 3버튼. 선택 표시(링). **전투 밖에서도** 표시·전환 가능.
- **입력**: `태세 공격` / `태세 공방` / `태세 방어` — **빙결·수면 중에도** 가능, 턴 소모 없음 (※ 단독 `[방어]`는 기존 가드 명령).
- **저장**: `SavedCharacter.battlePosture` 선택 필드. 전투 종료해도 태세 유지 (자동 초기화 없음).
- **스탯**: `getEffectiveStats`에서 항상 태세 배율 적용. 자동방어 확률은 적 턴(`triggerEnemyTurn`)에서만 의미 있음.
- **비전투**: `태세 …`만 입력한 경우 비전투 HP/MP 패시브 회복 틱을 돌리지 않음 (조작만 한 것으로 취급).

## 2026-03-23 — StatusBar EQUIPMENT: 내구도 표시

- **파일**: `frontend/src/components/StatusBar.tsx`, `frontend/src/App.tsx`
- **WHY**: 장비 줄에 이름만 있으면 내구·파손 상태를 HUD에서 바로 알기 어렵다.
- **동작**: 무기·보조손·방어구·반지·목걸이 슬롯에 장비가 있으면 **이름 앞(같은 줄)**에 `현재/최대` 표시(패널 세로 높이 축소 → HP 가림 완화). **최대 대비 20% 미만**이면 수치 빨간색; `0/…`은 추가로 굵게(파손). `formatHudSlotDurability`는 `weapon`/`armor`/`shield`/`accessory` 타입만 표시.

## 2026-03-23 — 스킬바: 호버 시 효과 설명

- **파일**: `frontend/src/data/skillTooltips.ts` (신규), `frontend/src/components/SkillBar.tsx`, `frontend/src/App.tsx`
- **WHY**: MP 숫자만으로는 스킬 효과를 기억하기 어렵다.
- **동작**: 각 스킬 버튼 호버 시 스킬명 + 효과 요약(기존 `스킬창`과 동일 문구) + 사용 조건(발동/MP 부족/비전투 제한). `title` 속성으로 브라우저 기본 툴팁 보조. 미등록 스킬은 `스킬창` 안내 문구.
